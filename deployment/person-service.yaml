apiVersion: apps/v1
kind: Deployment
metadata:
  name: person-service
  labels:
    app: person-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: person-service
  template:
    metadata:
      labels:
        app: person-service
    spec:
      containers:
      - name: person-service
        image: person-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 5001
        env:
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: udaconnect-db-secret
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: udaconnect-db-secret
              key: password
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: udaconnect-db-config
              key: host
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: udaconnect-db-config
              key: port
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: udaconnect-db-config
              key: name
---
apiVersion: v1
kind: Service
metadata:
  name: person-service
spec:
  selector:
    app: person-service
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
